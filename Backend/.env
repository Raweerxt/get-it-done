# .env

# --- Database Configuration (สำหรับ PostgreSQL) ---
DB_NAME=getitdone_db           # ชื่อ Database ที่คุณสร้างใน PostgreSQL
DB_USER=postgres               # User ของ Database (มักจะเป็น postgres)
DB_PASS=6530300066_6530300449   # ⚠️ **สำคัญ:** เปลี่ยนเป็นรหัสผ่านจริงของคุณ
DB_HOST=localhost         # ที่อยู่ของ Database (ถ้าคุณรันบนเครื่องตัวเอง)
DB_DIALECT=postgres            # ใช้ postgres

# --- Server Configuration ---
PORT=8000                    # Port ที่ Backend จะรัน

# --- Security Keys ---
JWT_SECRET=a_very_secret_key_for_jwt_signing_getitdone_6530300066_6530300449